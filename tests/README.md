# VoltAudit AI Test Suite

This directory contains the testing suite, including shared fixtures and unit/integration tests.

## Structure
- `conftest.py`: Global fixtures (e.g. database overrides, environment mocks).
- `unit/`: Unit tests validating functions and API routing logic.
